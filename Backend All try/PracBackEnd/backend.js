const http=require('http');
const server=http.createServer

(requestHandler);
const port=1000;
function requestHandler(req,res){
   switch:
   case: 

    }

server.listen(port,function(err){
    if(err){
        console.log('error',err);
        return;
    }
    console.log('sever is running fine', port)
    return;
    

});